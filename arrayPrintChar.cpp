#include <cstdio>
using namespace std;

int main( int argc, char ** argv) {
  char s[] = { 's', 't', 'r', 'i', 'n', 'g'};
  for (int i = 0; s[i];i++) {
    printf ("char  is: %c\n", s[i]);
  }
  return 0;
}
